# 更新紀錄

## 2026-09-08 v0.7.0

### 核准圖片與三態主視覺

- 採用使用者核准的四張原創示意圖：金屬粉末初態、真空燒結後完整零件、射出成型設備及真空燒結設備；保留既有滿意的主圖作為中間射出成形意象，不改動首頁版型、主要文案或品牌色。
- 首頁由單張海報加粒子效果改為三張實際圖片的階段切換；延續暫停／重播、鍵盤操作、離屏停止與減少動態效果處理，圖片採響應式 WebP。
- 首頁製程與設備頁採用核准設備示意，延續 AVIF／WebP 響應式與延遲載入；中、英、日標題／替代文字清楚標示為示意，不宣稱為公司實機照片。原始照片及公開舊網址保留。
- 新圖原始 PNG 存於 `future-site/src/assets/images/`；粉末與成形階段為製程視覺意象，並非物理模擬或各階段實拍；真空爐保留密閉圓形爐門與管路，不採用連續爐造型。

### 載入與驗證

- SSR 保留完整燒結零件，無 JavaScript 時仍可顯示；正常模式解碼粉末圖後開始播放，射出態首次需要時才載入。減少動態／Save-Data／無 Canvas 時停留靜態並可手動切換，不自動請求其他階段。
- 增加每階段解碼快取、同階段載入去重、失敗重試與過期請求保護；修正粉末階段重播計時，並在圖片本身的留白邊缘做透明漸隱，避免切換層出現深色矩形。
- Astro check：59 檔案，0 errors／warnings／hints；根路徑及 GitHub Pages 子路徑各完成 57 頁建置與資源掃描，缺失 0。
- 非同步生命週期 Node VM 測試 9/9 通過；實際 IAB 檢查三語、三態圖片、暫停／重播及鍵盤操作，並檢查 1440×900、910×787、390×844、320×568 響應式尺寸。偏好設定及網路異常以 source-level mock 覆蓋，不宣稱已在真實行動網路或其他瀏覽器測試。
- Hero JS 約 7.1 KB／gzip 3.1 KB；960w 三態 WebP 約 120.9／86.8／29.0 KB；兩張設備的 500w AVIF 約 7.3／13.4 KB，原始 PNG 不直接作為頁面傳輸圖片。

## 2026-09-07 v0.6.1

- 最終色彩複核後，將深色區域的小字紅色調整至 `#ff606d`、紙白區域調整至 `#bd0010`，對比值分別約 6.51:1 與 5.97:1；品牌標誌與主要按鈕仍保留震欣紅。
- 主要按鈕 hover 改為較深的紅色，避免白色按鈕文字對比降低。此修正不改動版型、文案或互動邏輯。

## 2026-09-07 v0.6.0

### 視覺與互動改造

- 採用確認後的方案 1：石墨／紙白交替版面、精簡導覽、連續能力佐證列、四步製程、材料與樣品滑軌、應用案例帶及聯絡區，並統一三語與內頁樣式。
- 以震欣既有零件作為造型參考，製作原創粉末凝聚概念主圖；改用輕量 Canvas2D，提供暫停、重播、手動階段與鍵盤操作，保留離屏／頁籤停止、靜態 fallback 與資源清理。
- 製程改用實際真空燒結爐照片，並同步修正中、英、日首頁、製程頁與設備頁說明；不使用連續爐示意。日文保留ハイス鋼（HSS）在地用法。
- 詢問表單可整理與預覽本機草稿、開啟郵件草稿或下載文字；明確告知尚未寄出，不將輸入資料傳送到伺服器或儲存。
- 修正手機語言選單裁切、Esc 焦點、跨斷點狀態與最窄視窗溢出；補上無 JS 導覽與詢問聯絡 fallback。

### 驗證

- Astro check：59 個檔案，0 errors、0 warnings、0 hints；根路徑與 GitHub Pages 子路徑均完成 57 頁建置。
- 根路徑建置的本機連結、圖片與 srcset／hash 掃描無缺失；Canvas 生命週期的 6 組 Node VM 測試通過。
- 已進行三語首頁、日文製程／HSS 內頁及桌面／手機瀏覽器檢查；詳細證據、測試範圍與限制見 `design-qa.md`。
- 動畫模組建置大小約 5.13 KB、gzip 約 2.36 KB；主圖三種 WebP 尺寸約 41.6／86.8／154.3 KB，未載入 Three.js。

## 2026-09-07 v0.5.2

### 修正

- 將 Linux 原生套件安裝與 Sharp 載入驗證整合為專案腳本，讓 GitHub Pages 與 Vercel 使用同一份已版控邏輯。
- 將 Vercel `installCommand` 縮短為呼叫共用腳本，修正 v0.5.1 指令長度 827 字超過 Vercel schema 256 字上限，導致建置前即被拒絕的問題。

### 驗證

- `vercel.json` 可通過官方 schema 的核心長度限制，且 GitHub Actions 仍保留獨立的 Linux 原生套件関門。
- 共用安裝腳本已通過 Node 語法與本機非 Linux 安全跳過檢查；Astro 58 個檔案為 0 errors、0 warnings、0 hints，並完成 GitHub Pages 子路徑模式的 57 頁建置。

## 2026-09-07 v0.5.1

### 修正

- 將 Sharp 0.34.5 改為網站的明確相依，避免 Astro 圖片管線只透過可選相依載入影像引擎。
- GitHub Pages 與 Vercel 在 Linux 建置時會依專案版本顯式安裝 Sharp x64 glibc 原生套件，並在檢查與建置前執行載入驗證。
- GitHub Pages 基礎相依安裝改用 `npm ci`，使部署必須與已提交的 lockfile 一致。
- GitHub Actions 的 npm 快取鍵改以 workspace 實際使用的根目錄 `package-lock.json` 為準，避免快取與實際安裝來源不一致。
- GitHub Actions 的 checkout 與 Node 設定動作升級至 v5，移除舊版 Node 20 action runtime 的停用警告；網站建置版本仍固定為 Node 22。

### 驗證

- 已從 GitHub Actions 完整 log 確認 v0.5.0 失敗點為「產生最佳化圖片」階段無法載入 Sharp，非記憶體耗盡或頁面程式錯誤。
- 已確認兩份 lockfile 與套件版本一致、Sharp 可於本機載入，並通過 GitHub Pages 子路徑模式的 `astro check` 與 57 頁正式建置。

## 2026-09-07 v0.5.0

### 效能優化

- 將 14 張產品、材料、設備、品質與型錄圖片納入 Astro 圖片管線，依畫面寬度輸出響應式 AVIF，並以 WebP 作為 fallback；同時補上 `srcset`、`sizes`、固有尺寸、延遲載入與非同步解碼。
- 保留既有 `public/assets` 原始網址以避免舊連結失效，並限制產生尺寸不得超過來源寬度，避免無效放大與多餘傳輸。
- 依裝置能力調整首頁 Three.js 畫布像素密度、幾何複雜度與粒子量；離開視窗或頁籤時暫停動畫，返回後恢復，並支援省流量與減少動態效果偏好。
- 補齊 Three.js observer、事件與 WebGL 資源釋放流程，並修正首幀時間差可能造成輪播索引失效的問題。

### 日文在地化

- 將一般頁面與導覽中的「高速度鋼」改為日本業界較自然的「ハイス鋼（HSS）」；首次說明保留正式名稱「高速度工具鋼」，兼顧專業性與搜尋辨識。
- 全面複核日文首頁、公司、MIM 製程、材料、應用、設備、品質、型錄與聯絡頁，修正直譯語氣、製程因果、工業術語、導覽名稱與日文頁尾地址。
- 補齊三語導覽、語言選單、頁尾與 3D 畫布的本地化無障礙標籤。

### 驗證

- 已在 Windows Chrome 以 1440×900 與 390×844 視窗驗證繁中、英文、日文首頁及日文材料頁；10 組情境均無破圖、console error、page error、request failure 或非預期 HTTP 回應。
- 首頁 19 張內容圖片均由瀏覽器選用 AVIF；本機桌機完整滾動的資源 encoded body 約 373 KB，手機首屏未預載產品圖片。
- 已確認 Three.js 可見時持續繪製、離屏時停止、返回後恢復，且 `prefers-reduced-motion` 模式僅輸出靜態畫面。
- 已執行備份完整性與 ZIP 雜湊驗證，並執行 `npm run check` 與 `npm run build`。

## 2026-09-05 v0.4.3

### 調整

- 備份改為依 Git 追蹤檔案建立完整清單，涵蓋素材庫、GitHub 部署設定、Vercel 設定與網站原始碼；`.env.example` 可納入，其他環境檔、密鑰與資料庫仍排除。
- ZIP 驗證逐一讀取每個檔案並以 SHA-256 比對 staging 與來源內容，同時檢查追蹤檔、必要部署項目與素材庫是否完整。
- 修正 `.gitattributes`：CSV 固定 LF、PowerShell／批次檔固定 CRLF、SVG 固定 LF、51 個大寫 `.JPG` 與既有素材二進位檔不做文字轉換，並以精確路徑保留三個 legacy 首頁 TXT 的原始位元組。

### 驗證

- 已執行備份完整性與 ZIP 雜湊驗證，確認符合條件的追蹤檔均已收錄且 SHA-256 mismatch 為 0。
- 已執行 lockfile 一致性檢查、`npm run check` 與 `npm run build`。

## 2026-05-22 v0.4.2

### 調整

- 修正 1400px 以下響應式導覽展開時仍沿用桌機 `max-width`，導致選單未貼齊右側與未隨視窗滿版縮放的問題。
- 將頁首語言選取按鈕改為配合震欣紅、科技藍與玻璃頁首的流動光暈樣式，保留語言選單互動。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。
- 已在瀏覽器以 1129px 寬度驗證日文頁首展開選單滿版貼齊，並確認語言按鈕樣式與選單可正常開啟。

## 2026-05-22 v0.4.1

### 調整

- 調整根網址語言 redirect 規則：日文仍進 `/ja/`、中文仍進 `/zh/`，英文與主要歐美語系改進 `/en/`。
- 保留無法偵測語言或不在上述語系清單時預設進 `/zh/` 的行為。
- 同步更新非 Vercel 環境下的根網址 fallback 判斷邏輯，讓本機與正式部署規則一致。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.4.0

### 新增

- 新增完整日文版網站路由 `/ja/`，包含首頁、公司簡介、MIM 製程、材料能力、材料明細、應用案例、製程能力、品質控管、電子型錄與聯絡頁面。
- 將共用網站資料、材料資料、應用案例、表單與頁尾文案擴充為繁體中文、英文、日文三語。

### 調整

- 將根網址 `/` 改由 Vercel 依瀏覽器第一順位 `Accept-Language` 正式 redirect 至 `/ja/`、`/zh/` 或 `/en/`，預設回 `/zh/`。
- 移除原本根網址只用白頁與 meta refresh 顯示「前往中文版」的中介體驗，改為非 Vercel 環境下的輕量語言 fallback。
- 將頁首語言切換改為三語選單，避免新增日文後導覽列在桌機寬度擠壓或換行。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.3.17

### 調整

- 將瀏覽器分頁 favicon 從黑底紅色 T 圖示改為震欣科技 J.S 菱形公司 Logo。
- 更新 favicon 引用版本參數，避免瀏覽器沿用舊圖示快取。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.3.16

### 調整

- 新增 `素材庫` 作為專案集中素材資料夾，將原本散落於根目錄的公司簡介、品質管制、客製服務、製程能力、電子型錄、首頁等原始素材移入分類資料夾。
- 將根目錄的公司型錄、簡報、PDF、Word 文件與交接參考圖整理到 `素材庫/02_型錄簡報與文件` 與 `素材庫/03_交接與參考`。
- 新增 `素材庫/README.md` 與 `素材庫/00_目錄索引/素材清單.csv`，方便後續查找與維護素材來源。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.3.15

### 調整

- 將頁首導覽列文字從原本 `12.5px` 放大為 `18.75px`，調整為 1.5 倍大小。
- 調整放大後的導覽列排列與響應式切換門檻，避免較窄桌機寬度出現換行擠壓。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.3.14

### 調整

- 針對頁首導覽列滑鼠移入時仍出現淺色矩形閃爍的問題，移除頁首與響應式導覽選單的即時 `backdrop-filter`，改用靜態玻璃漸層與隔離層處理，避免 Chromium 在 hover 重繪時產生濾鏡重繪矩形。
- 調整導覽列 hover / active 回饋，避免背景或邊框區塊隨項目寬度重繪。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.3.13

### 調整

- 移除桌機導覽列連結 hover 時的淡色矩形背景，避免滑鼠移入頁面時在玻璃頁首上閃現寬度不一的淺框。
- 保留導覽列 hover / active 的文字變色與底線提示，讓互動回饋不再造成視覺閃爍。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-22 v0.3.12

### 調整

- 將頁首公司名稱由圖片改為真正文字，避免縮放後產生模糊，並提升可讀性與可存取性。
- 將頁首左側 J.S Logo 改用高解析透明背景資產 `tanchin-logo-emblem-hires.png`。
- 修正高解析 Logo 在頁首被通用圖片樣式撐大的問題，並驗證桌機與手機版不重疊、不產生水平溢出。

### 備份與部署

- 已依工作流程執行核心資產備份。
- 已執行 `npm run check` 與 `npm run build`；建置通過，僅保留既有 Vite chunk size 提醒。

## 2026-05-21 v0.3.11

### 調整

- 新增 SVG 版官方 Logo 圖示與公司名稱圖，頁首改用向量資產顯示，避免放大後產生模糊。
- 中文電子型錄改連至 `tanchin-company-catalog-zh.pdf`，英文電子型錄改連至 `tanchin-company-catalog-en.pdf`。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.10

### 調整

- 將本機參考用 DNS 截圖與首頁參考圖片加入 `.gitignore`，避免誤上傳到 GitHub。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.9

### 調整

- 將頁首公司 Logo 整組放大，包含左側 Logo 圖示與右側公司名稱圖，提升第一眼品牌辨識度。
- 調整桌機導覽列字級與間距，讓放大後的 Logo 仍能與導覽列維持同一列且不產生水平溢出。
- 強化頁首玻璃擬態效果，改為更透明的層疊背景、背景模糊、飽和度與內側高光，讓底層宇宙背景更有透視感。
- 手機與平板版保留自適應 Logo 尺寸，避免放大後擠壓漢堡選單。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.8

### 修正

- 恢復首頁 Canvas 原本的抽象金屬 3D 核心視覺，避免前一版自生成零件取代原始主視覺。
- 移除照片貼圖與仿照片零件模型方向，保留原本較成熟的科技金屬風格。

### 新增

- 在首頁 Canvas 加入真正的 3D 輪播效果，第一頁為原本金屬核心，第二頁為同風格的金屬流線變化。
- 新增輪播淡入淡出、右上輪播指示點，以及隨輪播切換的下方狀態文字。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.7

### 調整

- 移除首頁 Canvas 中不自然的照片貼圖式 3D 呈現，改為 Three.js 程序化生成的金屬流線零件。
- 新增由弧形薄壁、尖長流線腳、加強肋、橢圓孔與拋光高光組成的自生成 3D 金屬模型，參考「精密曲面流線」照片的零件語彙。
- 調整金屬材質、補光與高光，提升不鏽鋼質感與旋轉時的光澤辨識度。
- 更新首頁 3D 視覺狀態文字為自生成金屬 3D 模型。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.6

### 修正

- 修正首頁 Three.js 3D 金屬視覺在部分載入時機下沒有初始化的問題，改為 DOM 就緒後啟動，並在減少動態模式下至少渲染靜態畫面。
- 重啟本機預覽環境後確認 Three.js 依賴可正常載入，首頁 3D 旋轉視覺恢復顯示。

### 調整

- 移除首頁首屏背景中的直線與橫線格線，改以星點與淡淡星雲光帶呈現。
- 調整星點分布為不等距位置，降低規律排列感。
- 保留全站背景由上到下的深淺色調漸層，維持深空層次感。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.5

### 調整

- 將全站背景統一為同一套深空宇宙底色，加入星點與星雲光帶，減少只有線條的單調感。
- 首頁首屏改為沿用全站深空背景，只保留必要的科技格線與 3D 金屬視覺。
- 移除產品圖展示位置的白色底板，改為透明深空展示底與陰影，讓去背零件更自然地融入網站。
- 依透明像素邊界裁切所有 `future-site/public/assets/products/*.png`，保留透明邊距但移除過大的透明留白，讓零件上站後更完整、更清楚。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.4

### 修正

- 移除首頁首屏 3D 視覺區塊中的半透明產品 fallback 圖，避免產品圖片以隱約方式藏在 Banner 背景裡。
- 清除 `scene-fallback` 相關樣式，讓首屏右側只保留 3D 金屬視覺與背景效果。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.3

### 調整

- 將新版網站頁首從深色導覽列調整為淺色玻璃感背景，減少灰暗、沉重的第一印象。
- 提高首頁首屏背景亮度，改為較明亮的藍灰金屬光感，保留科技製造氛圍。
- 調整導覽列、語言切換與手機選單的文字對比，讓淺色頁首下仍維持可讀性。

### 修正

- 改善手機導覽選單初始化方式，避免腳本太早執行時沒有掛上展開事件。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.2

### 調整

- 將新版網站頁首 Banner 的文字徽章改為引用舊站公司 Logo 原始圖檔：`logo.png` 與 `logo-name.png`。
- 新增 `future-site/public/assets/brand/tanchin-logo-emblem.png` 與 `future-site/public/assets/brand/tanchin-logo-name.png`，保留 Logo 原始顏色，不套用濾鏡或色彩修改。
- 調整頁首 Logo 的桌機與手機版尺寸，避免新版導覽列擠壓或溢出。

### 備份與部署

- 已依工作流程執行核心資產備份，並準備檢查、建置與推播。

## 2026-05-21 v0.3.1

### 修正

- 調整 `future-site/vercel.json` 的 `installCommand`，讓 Vercel 在 Linux 環境建置時補裝目前 Rollup 版本對應的 `@rollup/rollup-linux-x64-gnu`。
- 修正 Vercel 自動部署仍因 Rollup native package 缺漏而失敗的問題。

### 調整

- 更新 `docs/Vercel部署流程.md`，補充 Vercel install command 會處理 Rollup Linux 原生套件。

## 2026-05-21 v0.3.0

### 新增

- 完成 Vercel 與 GitHub 儲存庫的部署串接，讓 GitHub `main` 分支推播後可自動觸發 Vercel 部署。
- 新增 `future-site/vercel.json`，明確指定 Vercel 使用 Astro、`npm install`、`npm run build` 與 `dist` 輸出目錄。
- 新增 `docs/Vercel部署流程.md`，記錄 Vercel、GitHub、GitHub Pages 的角色分工與日後操作方式。

### 修正

- 修正 Vercel 初次部署時使用 workspace 根目錄 install command，造成 Rollup Linux optional dependency 缺漏而建置失敗的問題。

### 調整

- 更新 `.gitignore`，排除 `.vercel/` 本機連線資料，避免專案 ID 或本機部署資訊被提交。
- 更新 `docs/工作流程規則.md`，加入 Vercel 自動部署規則與正式網域建議方向。

### 備份與部署

- 本次修改完成後會執行核心資產備份、Astro 檢查、正式建置，並推播到 GitHub 觸發 Vercel 自動部署。

## 2026-05-21 v0.2.2

### 修正

- 進一步修正 GitHub Actions 的 Rollup Linux native dependency 問題。
- 部署流程會先讀取目前 Rollup 版本，再安裝相同版本的 `@rollup/rollup-linux-x64-gnu`，避免 CI 缺少 Linux 原生套件。

### 備份與部署

- 重新執行核心資產備份，並重新推送觸發 GitHub Pages 部署。

## 2026-05-21 v0.2.1

### 修正

- 修正 GitHub Actions 在 Linux runner 上執行 `npm ci` 時，Rollup optional dependency 未安裝導致 `astro check` 失敗的問題。
- 將 GitHub Pages 部署流程的依賴安裝步驟調整為 `npm install --include=optional`，讓 CI 能補齊平台原生套件。

### 備份與部署

- 重新執行核心資產備份，並準備重新推送觸發 GitHub Pages 部署。

## 2026-05-21 v0.2.0

### 新增

- 建立 Git 專案基礎設定，準備連線 GitHub 儲存庫 `Tan-Chin-Offical-Web-site`。
- 新增 GitHub Pages 自動部署工作流程，從 `future-site` 建置並發布靜態網站。
- 新增根目錄 `package.json`，提供統一的 `dev`、`build`、`check`、`preview`、`backup` 指令。
- 新增專案備份腳本 `scripts/backup-core-assets.ps1`。
- 新增工作流程規則文件 `docs/工作流程規則.md`。
- 新增 `.gitattributes`，固定程式與文件類檔案的換行格式。

### 調整

- 調整 Astro 設定，支援 GitHub Pages 專案子路徑與正式網域兩種部署情境。
- 補齊 `.gitignore`，避免上傳 `node_modules/`、`dist/`、log、備份、驗證截圖與敏感設定。

### 備份與部署

- 本次會執行核心資產備份，備份檔放在 `_backups/`，該資料夾不納入 Git。
- GitHub Pages 先作為第一階段部署方式，未來可再接 Vercel。
